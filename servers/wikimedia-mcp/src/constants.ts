/** The credentials-table partition key — must exactly match apps/portal/src/manifest.ts's `slug`. */
export const SERVER_SLUG = "wikimedia-mcp";

/** Optional Wikimedia OAuth 2.0 access token — see src/credentials.ts for why it's optional. */
export const WIKIMEDIA_OAUTH_TOKEN = "WIKIMEDIA_OAUTH_TOKEN";
