{
  "name": "@iolab/ia-mcp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "MCP server for Internet Archive / Wayback Machine research: find and read archived web pages, track how pages and sites changed over time, search archive.org's item corpus, and read full text of scanned public-domain books. All upstream APIs are public; optional archive.org S3-style credentials are accepted but no tool requires them.",
  "exports": {
    ".": "./src/agent.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@iolab/credentials": "workspace:*",
    "@iolab/mcp-kit": "workspace:*",
    "@modelcontextprotocol/sdk": "1.29.0",
    "agents": "^0.17.3",
    "zod": "^4.1.10"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.110.0"
  }
}
