{
  "name": "@iolab/credentials",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Shared D1-backed encrypted credential store: the portal writes upstream API keys here, MCP servers read them at request time. Neither the repo maintainer nor Claude ever sees plaintext values in transit.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
