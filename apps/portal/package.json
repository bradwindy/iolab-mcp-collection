{
  "name": "@iolab/portal",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Credential-management dashboard for the collection: set/update each MCP server's upstream API keys, view credential status, and get ready-to-run `claude mcp add` commands for every server. Mounted by the gateway under /admin, no longer independently deployable.",
  "exports": {
    ".": "./src/app.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@iolab/credentials": "workspace:*",
    "@iolab/mcp-kit": "workspace:*",
    "hono": "^4.12.28"
  },
  "devDependencies": {
    "jose": "^6.2.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.11"
  }
}
